# ZIPを解凍した場所へ移動
cd stripe-shop

# Gitの初期設定（初回のみ）
git config --global user.name "diegocode"
git config --global user.email "diegoosawa@gmail.com"

# GitHubにプッシュ
git init
git add .
git commit -m "first commit"
git remote add origin https://github.com/あなたのユーザー名/stripe-shop.git
git push -u origin main
